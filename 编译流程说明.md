# 编译流程说明

本项目的方向是**中间代码生成汇编**，汇编是流水线终点。不存在"汇编代码生成中间代码"的环节。

## 完整流水线

```
源码 .c
  │  scanner.l（词法分析）
  ▼
Token 流
  │  parser.y（语法分析，构建 AST）
  ▼
AST（abstract_syntax_tree/）
  │  interCode.cpp（Generate → 四元式）
  ▼
中间代码 quad_list（intermidiate_code/）
  │  interCode.cpp 的 optimize()   ← 阶段6 的优化就发生在这里
  ▼
优化后的四元式
  │  AsmGenerate.cpp（new AsmGenerate(quadlist, table) → generate()）
  ▼
x86 汇编 asm/test.asm（NASM 语法）
  │  nasm 汇编 + gcc 链接
  ▼
32 位可执行文件
```

对应代码位置（`parser.y:919-922`）：

```cpp
//parser.y
//AsmGenerate* asmgenerate = new AsmGenerate(interCode.getQuadlist(), interCode.getTempVars(), interCode.getTable(), interCode.getFuncTable());
AsmGenerate* asmgenerate = new AsmGenerate(interCode.getQuadlist(),  interCode.getTable());
asmgenerate->generate();
```

## 三个关键点

1. **有生成汇编代码吗？**
   有。`asm/AsmGenerate.cpp` 消费优化后的四元式 `quad_list`，输出 `asm/test.asm`（32 位 x86、NASM 语法，含寄存器分配 eax/ebx/ecx/edx）。

2. **中间代码后续优化吗？**
   有，而且是**生成汇编之前**做。`interCode.cpp` 的 `Root_Generate()` 顺序是：

   ```cpp
   //intermidiate_code/interCode.cpp
   void InterCode:: Root_Generate(){
       std::cout<<"Gen "<<root->content<<std::endl;
       Generate(this->root, this->rootTable);
       // 阶段6：四元式级优化（常量折叠/死代码消除/不可达删除）
       this->optimize();
   ```

   这属于教科书里的**中间代码级优化**（在 `AsmGenerate` 之前执行，`AsmGenerate` 构造时拿到的是已优化列表）。

3. **是汇编代码生成中间代码还是？**
   反过来才对。汇编是由中间代码生成的，汇编之后没有"再生成中间代码"的环节。汇编 `test.asm` 再经 `nasm`/`gcc` 变为目标文件 `.o` 和最终可执行文件（对应 `CMakeLists.txt` 里的 `asm_io.o`/`test.o`/`test_executable`）。

## 一句话记忆

**AST → 四元式 →（优化）→ 汇编 → 机器码**，优化只发生在四元式阶段，汇编是单向输出。
